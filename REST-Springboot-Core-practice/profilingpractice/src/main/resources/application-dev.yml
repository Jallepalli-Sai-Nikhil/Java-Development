spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/user_dev?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Nikhil@420}

  jpa:
    show-sql: true