spring:
  application:
    name: global-exception-handler

  datasource:
    url: jdbc:mysql://localhost:3306/global_exception_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Nikhil@420  # change to your MySQL password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # creates/updates tables automatically
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always